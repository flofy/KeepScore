# PWA notes

The manifest is intentionally minimal for the first PWA iteration.

Before shipping an installable production build, add generated platform icons (192px and 512px), a service worker, and an offline app-shell strategy.
